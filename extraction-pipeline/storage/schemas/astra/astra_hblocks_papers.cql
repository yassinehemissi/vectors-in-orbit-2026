CREATE TABLE IF NOT EXISTS "Experimentein".hblocks_papers (
  paper_id TEXT,
  title TEXT,
  paper_url TEXT,
  summary TEXT,
  summary_chars INT,
  source_section_count INT,
  PRIMARY KEY (paper_id)
);
